# Development-only second telemetry link on the ESP32-P4 USB-Serial-JTAG port,
# selected by the "Second Link" IDE tasks. The device configuration contract
# knows nothing about it, so a product build simply omits this fragment.
CONFIG_SIMCORE_SECOND_TELEMETRY_LINK=y
